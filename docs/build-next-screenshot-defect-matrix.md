# Build Next Screenshot Defect Matrix

Date: 2026-07-19  
Target: next unused iOS build after Build 39, pending final EAS re-check

| Screenshot | Visible defect | Underlying system defect | Affected state source | Correction | Validation | Final status | Device-only risk |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Photo 1 | Race and program dates were free-text ISO-style fields and opened the keyboard. | Intake and secondary settings screens still exposed storage format to the athlete. | Onboarding goal/date state, Training Preferences race settings, calibration PRs, manual workout logging. | Onboarding goal uses MM-DD-YYYY display and `MultiColumnPickerSheet` month/day/year selection while storing ISO internally. Settings, Profile calibration, and Log Workout now expose MM-DD-YYYY display/input and normalize to ISO before persistence. | `buildNextUiAndControls.test.ts`, typecheck. | Fixed. | Native picker feel still requires device pass. |
| Photo 2 | PR finish time used plus/minus controls. | Race PR entry had bespoke steppers instead of the shared picker-sheet pattern. | Onboarding PR calibration state. | Finish time uses Hours / Minutes / Seconds picker columns with Cancel/Confirm. | `buildNextUiAndControls.test.ts`. | Fixed. | Physical dynamic-type/VoiceOver check remains. |
| Photo 3 | Activity showed seeded-looking skiing/cycling/strength/mobility history. | Fresh Activity defaults needed explicit no-seed guard and meaningful-activity filtering. | Activity store and Activity history screen. | Verified production store defaults to `activities: []`; empty state says completed work will appear after real training. | `buildNextStabilization.test.ts`, Activity tests. | Fixed in source; screenshot records are treated as previously logged device data. | User device may still contain old local records until app data is cleared/migrated manually. |
| Photo 4 | Coach input/keyboard left large unusable blank space and message area did not stay anchored. | Chat ScrollView lacked interactive keyboard dismissal and full handled taps. | AI Coach screen layout. | Added interactive keyboard dismissal, handled taps, scroll-dismiss behavior, and bottom-safe input row. | `buildNextUiAndControls.test.ts`, code inspection, Expo SDK 56 keyboard guidance. | Fixed. | Exact keyboard animation requires TestFlight device validation. |
| Photo 5 | Coach displayed raw markdown (`**...**`) and invented/check-the-app wording. | Coach response display lacked sanitization and prompt did not forbid raw syntax strongly enough. | AI Coach prompt and bubble renderer. | Added `sanitizeCoachDisplayText`, no emoji/markdown instructions, and exact scheduled-session context. | `buildNextStabilization.test.ts`, `buildNextUiAndControls.test.ts`, prompt-budget tests. | Fixed. | Remote model style still needs spot-check; display sanitizer protects visible UI. |
| Photo 6 | Calendar strength card showed generic `Strength / Upcoming`. | Calendar displayed raw/generated entry summary instead of scheduled-session detail. | Calendar / scheduled-session adapter. | Calendar uses scheduled sessions and shows duration, type, exercise count, designation, and contextual action. | `buildNextScheduleIntegration.test.ts`, Calendar tests. | Fixed. | Final visual spacing needs device review. |
| Photo 7 | Same-day duplicate beginner runs appeared and current-day workouts showed Missed too early. | Calendar, Running, and Today read competing schedule sources; date fixtures could mark past UTC days. | Training schedule, Calendar, Running. | `ScheduledSession` adapter dedupes primary endurance sessions and never marks today Missed before end of day. | `buildNextScheduleIntegration.test.ts`, `buildNextStabilization.test.ts`. | Fixed. | Local timezone behavior should be checked on device after install. |
| Photo 8 | Running Plan showed Rest Day while Calendar showed runs. | Running tab read `calendarMap`/`richWeek` directly instead of the scheduled-session adapter. | Running Plan. | Running Plan now uses `useScheduledSessions(weekPlan)` for run/run-walk/walk lookup. | `buildNextScheduleIntegration.test.ts`, typecheck. | Fixed. | Device state with previously generated plans should be checked. |
| Photo 9 | Running Active showed generic/truncated Easy Run instead of exact prescription. | Active run selected raw `calendarMap` workout and did not receive beginner prescription. | Running Active / scheduled workout. | Active tab resolves today’s scheduled run/run-walk/walk session and uses the rich workout interval prescription. | `buildNextScheduleIntegration.test.ts`, run/walk tests. | Fixed. | Actual run start/voice prompts require physical GPS validation. |
| Photo 10 | Today strength card used generic “View Activity” and weather lacked AQI. | Today mixed scheduled sessions with beginner-plan fallback and weather had no AQI companion fetch. | Today, weather data. | Today reads scheduled sessions, uses exact action labels, and displays AQI or AQI unavailable with guidance sheet. | `weatherLogic.test.ts`, `buildNextScheduleIntegration.test.ts`. | Fixed. | AQI depends on provider availability/location permission. |

## Visual validation boundary

The supplied physical-device screenshots were inspected directly. Local simulator rendering was unavailable because the host has Command Line Tools selected rather than full Xcode, and no browser renderer was available. Source-level visual QA is backed by deterministic layout/route/state tests plus iOS Metro export; final keyboard, Live Activity, GPS, and VoiceOver confirmation remains a TestFlight device checklist item.
